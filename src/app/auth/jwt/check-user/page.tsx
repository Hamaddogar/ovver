// sections
import { JwtCheckUserView } from 'src/sections/auth/jwt';

// ----------------------------------------------------------------------

export const metadata = {
    title: 'Jwt: Check User',
};

export default function CheckUserPage() {
    return <JwtCheckUserView />;
}
